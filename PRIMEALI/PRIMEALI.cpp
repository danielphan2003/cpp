#include <cstdio>
#include <iostream>

using namespace std;

float N;

int main() {
    freopen("PRIMEALI.INP", "r", stdin);
    freopen("PRIMEALI.OUT", "w", stdout);

    cin >> N;
    N = sqrt(N);
    long long n =
}