#include <cstdio>
#include <iostream>
#include <queue>

using namespace std;

int n, v[1010] = {0};
queue<int> q;

int main() {
    freopen("GIFTS.INP", "r", stdin);
    freopen("GIFTS.OUT", "w", stdout);

    cin >> n;
    for (int i = 1; i <= n; ++i)
        cin >> v[i];

    max = 0;
    last = 0;
    for (int i = 1; i <= n; ++i) {
        q.push(v[i]);
        if
    }

    return 0;
}